package com.js.interpreter.runtime.exception;

/**
 * The stack has grown beyond its maximum size
 * (in which case the size of local variables should be reduced to avoid this error),
 * or the stack has become corrupt.
 * This error is only reported when stack checking is enabled.
 * Created by Duy on 07-Apr-17.
 */
public class StackOverFlowError {
}
