<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.duy.pascal.compiler">

    <uses-permission android:filePath="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:filePath="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:filePath="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:filePath=".activities.EditorActivity"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout"
            android:label="@string/app_name"
            android:launchMode="singleTask" />
        <activity
            android:filePath=".activities.ActivitySplashScreen"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout"
            android:label="@string/app_name">
            <intent-filter>
                <category android:filePath="android.intent.category.DEFAULT" />
                <category android:filePath="android.intent.category.LAUNCHER" />

                <action android:filePath="android.intent.action.MAIN" />
            </intent-filter>
            <intent-filter>
                <action android:filePath="android.intent.action.VIEW" />

                <category android:filePath="android.intent.category.DEFAULT" />
                <category android:filePath="android.intent.category.BROWSABLE" />

                <data android:scheme="file" />
                <data android:mimeType="*/*" />
                <data android:host="*" />
                <data android:pathPattern=".*\\.pas" />
            </intent-filter>
            <intent-filter>
                <action android:filePath="android.intent.action.VIEW" />

                <category android:filePath="android.intent.category.DEFAULT" />

                <data android:scheme="file" />
                <data android:mimeType="text/plain" />
            </intent-filter>
            <intent-filter>
                <action android:filePath="android.intent.action.SEND" />

                <category android:filePath="android.intent.category.DEFAULT" />

                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>
        <activity
            android:filePath=".activities.ExecuteActivity"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout"
            android:icon="@drawable/ic_code_white_24dp"
            android:launchMode="singleTask"
            android:windowSoftInputMode="stateVisible|stateAlwaysVisible" />
        <activity
            android:filePath=".activities.DocumentActivity"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout"
            android:icon="@drawable/ic_info_white_24dp" />
        <activity
            android:filePath=".activities.SettingsActivity"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout"
            android:icon="@drawable/ic_info_white_24dp" />
        <activity
            android:filePath=".activities.CodeSampleActivity"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout"
            android:icon="@drawable/ic_code_white_24dp"
            android:label="@string/title_activity_code_sample" />
    </application>

</manifest>