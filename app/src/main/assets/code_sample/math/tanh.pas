Program Example48;

{ Program to demonstrate the Tanh function. }

Uses math;

begin
    writeln(tanh(0));
    writeln(tanh(1));
    writeln(tanh(-1));
end.
{http://www.freepascal.org/docs-html/rtl/math/tanh.html}